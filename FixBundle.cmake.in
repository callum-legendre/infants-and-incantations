include(BundleUtilities)

set(APP_BUNDLE
    "${CMAKE_INSTALL_PREFIX}/iai.app"
)
get_filename_component(APP_BUNDLE "${APP_BUNDLE}" ABSOLUTE)

message("Fixing bundle: ${APP_BUNDLE}")

fixup_bundle(
    "${APP_BUNDLE}"
    ""
    "/Users/callumlegendre/Documents/GitHub/infants-and-incantations/build/_deps/ogre-build/lib/macosx"
)
